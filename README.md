# Maze Generator

This is a javascript library, written in TypeScript that generates a maze data structure. 

## Install 

```
yarn add maze-generator-js --dev
```

## Usage

```
const mazegen = require('maze-generator-js')
const grid = mazegen.make({
    columns: 20,
    rows: 20
})
```